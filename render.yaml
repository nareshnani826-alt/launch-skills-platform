services:
  - type: web
    name: launch-skills-platform
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install && npm run db:push && npm run db:ensure-admin && npm run build
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: ADMIN_RESET_PASSWORD
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: NODE_VERSION
        value: 20.18.0
